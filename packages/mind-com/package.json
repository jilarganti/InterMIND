{
  "name": "mind-com",
  "description": "InterMIND brand campaign site (Nuxt)",
  "version": "0.6.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --dotenv .vercel/.env.development.local",
    "build": "nuxt build",
    "preview": "nuxt preview --dotenv .vercel/.env.development.local",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare",
    "lint": "pnpm eslint --cache .",
    "format": "prettier --cache --cache-strategy content -w .",
    "typecheck": "nuxt typecheck"
  },
  "dependencies": {
    "@nuxt/content": "^3.14.0",
    "@nuxtjs/robots": "^6.0.8",
    "@nuxtjs/sitemap": "^8.0.15",
    "@vueuse/nuxt": "^12.8.2",
    "better-sqlite3": "^12.10.0",
    "nuxt": "^4.4.6",
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "simple-git-hooks": "^2.13.1"
  }
}
