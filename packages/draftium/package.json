{
  "name": "draftium",
  "description": "Multilingual Video Meetings with Voice Interpretation",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "translate": "node --no-deprecation ../../scripts/translate.js ./scripts/translateConfig.js",
    "test:traffic-sources": "node --experimental-specifier-resolution=node --loader ts-node/esm __tests__/test-traffic-sources.ts",
    "format": "prettier -w .",
    "lint": "pnpm eslint --cache ."
  },
  "dependencies": {
    "shared": "workspace:*"
  }
}
